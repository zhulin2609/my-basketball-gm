spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/basketball_gm_dev}
    username: ${DB_USERNAME:zhulin}
    password: ${DB_PASSWORD:}

app:
  local-dev-owner-id: ${LOCAL_DEV_OWNER_ID:00000000-0000-0000-0000-000000000001}
